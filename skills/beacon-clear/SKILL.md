---
name: beacon-clear
description: Clear local Beacon pane state when the tmux display is stale.
---

Reset state through the public CLI so locking and file permissions remain correct:

```bash
beacon reset
```
